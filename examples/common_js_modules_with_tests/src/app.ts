
import U = require('./utils/utils');

// for node and mocha install these packets first:
// > npm install

var what : string = U.TotalBasicUtils.a();

(new U.TotalBasicUtils()).b();


console.log(what);